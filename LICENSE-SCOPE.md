# License scope

No additional license is granted by this branch.

The repository contains application code, generated XML documentation, and a
git submodule. The HTTP-status explanatory strings in
HttpStatusApi/Controllers/ and the generated files in HttpStatusApi/XmlFile/
are kept outside any proposed MIT scope until their provenance is resolved or
the text is replaced by an independently written, standards-based
description.

The BasicDomain submodule is a separate repository and is never relicensed by
this repository. Other package references are dependencies, not vendored
source. Any future MIT grant must name the exact files and preserve these
exclusions.
